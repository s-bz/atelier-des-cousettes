name: Daily rebuild

on:
  schedule:
    - cron: '0 6 * * *' # Every day at 6:00 UTC (8:00 Paris time)

jobs:
  rebuild:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Vercel deploy
        run: curl -s -X POST "${{ secrets.VERCEL_DEPLOY_HOOK }}"
